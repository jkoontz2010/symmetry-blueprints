import { Template } from "symmetric-parser/dist/src/templator/template-group"
import { TemplateAsString } from "./wordRunService";

export const get = async (configPath:string, input: TemplateAsString)=>{
    return input;
}

export const getOrCreate = async (configPath:string, input: TemplateAsString)=>{
    return input;
}